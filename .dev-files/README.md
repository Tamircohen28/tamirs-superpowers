# .dev-files/

Session-local development artifacts for contributors (plans, reviews, investigations, scratch).

This directory is git-ignored except this README. See `rules/dev/dev-files-workspace.md`.
