# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo

# Node / npm (in case any skill spawns local deps)
node_modules/
npm-debug.log*

# Local secrets / env
.env
.env.local
*.local

# Build / dist
dist/
build/
*.log

# Agent worktrees (repo-local) and session state
.claude/.worktrees/
.claude/worktrees/
.claude/outputs/
.claude/sessions/
.cursor/.worktrees/
.codex/.worktrees/

# Session-local dev artifacts (only .dev-files/README.md is tracked)
.dev-files/*
!.dev-files/README.md

# Session files (plans, reviews, investigations — visible but not committed)
session-files/
