# org-unreachable

`GET /orgs/{org}/rulesets` answers `500`. The only org-probe outcome that is a
real failure rather than a degrade — the answer is unknown, so nothing may be
concluded about the organization's plan or this token's permissions.
