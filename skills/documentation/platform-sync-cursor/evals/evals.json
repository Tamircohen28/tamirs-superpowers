{
  "skill_name": "platform-sync-cursor",
  "evals": [
    {
      "id": 1,
      "prompt": "Analyze this repo's Cursor configuration against the latest Cursor docs.",
      "expected_output": "Should NOT perform any analysis or fetching itself. Should state that platform-sync-cursor is a deprecated compatibility shim, invoke tamirs-superpowers:platform-sync scoped to the cursor target, and return that engine's Cursor section unchanged. Must not cite Cursor features from training knowledge.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Use platform-sync-cursor but the platform-sync skill is not available.",
      "expected_output": "Should report plainly that the platform-sync engine is unavailable and stop. Must not fall back to training knowledge, must not re-implement the analysis loop, and must not fetch docs.cursor.com, .cursor-plugin/plugin.json, .cursor/rules/*.mdc, .cursorrules directly.",
      "files": []
    }
  ]
}
