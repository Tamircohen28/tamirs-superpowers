# Docs Review — ${DATE}

_Generated by `.claude/skills/review-docs`. This file is itself a plan/work
file — untrack or move it once the user has acted on the findings._

## Summary

| Metric | Count |
|---|---|
| Files audited | ${FILES_AUDITED} |
| Visual fixes applied | ${VISUAL_FIXES} |
| Stale docs updated | ${STALE_UPDATED} |
| Plan/work files untracked | ${PLAN_UNTRACKED} |
| Template violations fixed | ${TEMPLATE_FIXED} |
| Broken links repaired | ${LINKS_REPAIRED} |

## Stale docs (Axis 2)

${STALE_TABLE}

## Stray plan/work files (Axis 3)

${PLAN_TABLE}

## Template violations (Axis 4)

${TEMPLATE_TABLE}

## Link / anchor / Mermaid issues (Axis 5)

${LINK_TABLE}

## Actions taken

${ACTIONS_LIST}

## Follow-ups for the user

${FOLLOWUPS_LIST}

---

After review:
- Confirm the findings.
- If everything looks right, untrack this report:
  ```bash
  git rm --cached docs/review-${DATE}.md
  ```
- If you want to keep a copy: `mv docs/review-${DATE}.md ~/production-master-notes/`.
