{
  "skill_name": "platform-sync-claude",
  "evals": [
    {
      "id": 1,
      "prompt": "Analyze this repo's Claude Code configuration against the latest Claude Code docs.",
      "expected_output": "Should NOT perform any analysis or fetching itself. Should state that platform-sync-claude is a deprecated compatibility shim, invoke tamirs-superpowers:platform-sync scoped to the claude-code target, and return that engine's Claude Code section unchanged. Must not cite Claude Code features from training knowledge.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Use platform-sync-claude but the platform-sync skill is not available.",
      "expected_output": "Should report plainly that the platform-sync engine is unavailable and stop. Must not fall back to training knowledge, must not re-implement the analysis loop, and must not fetch code.claude.com docs, .claude-plugin/, CLAUDE.md, .claude/rules/, .claude/skills/, skills/ + hooks/, .mcp.json, commands/ directly.",
      "files": []
    }
  ]
}
