{
  "skill_name": "platform-sync-codex",
  "evals": [
    {
      "id": 1,
      "prompt": "Analyze this repo's OpenAI Codex CLI configuration against the latest OpenAI Codex CLI docs.",
      "expected_output": "Should NOT perform any analysis or fetching itself. Should state that platform-sync-codex is a deprecated compatibility shim, invoke tamirs-superpowers:platform-sync scoped to the codex target, and return that engine's OpenAI Codex CLI section unchanged. Must not cite OpenAI Codex CLI features from training knowledge.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Use platform-sync-codex but the platform-sync skill is not available.",
      "expected_output": "Should report plainly that the platform-sync engine is unavailable and stop. Must not fall back to training knowledge, must not re-implement the analysis loop, and must not fetch github.com/openai/codex docs, .codex-plugin/plugin.json, AGENTS.md, .codex/ directly.",
      "files": []
    }
  ]
}
