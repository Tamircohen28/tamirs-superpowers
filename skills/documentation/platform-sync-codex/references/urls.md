# OpenAI Codex CLI — source URLs (moved)

This file is a pointer. The permitted OpenAI Codex CLI source URLs — P0, P1 and P2, with their
fetch conditions — now live in the platform reference data consumed by the `platform-sync`
engine:

**`skills/documentation/platform-sync/references/platforms/codex.md`** → "Sources" tables.

Nothing fetches from this file any more. `platform-sync-codex` is a deprecated shim that
delegates to `platform-sync` and performs no fetching of its own.
