{
  "skill_name": "platform-sync-opencode",
  "evals": [
    {
      "id": 1,
      "prompt": "Analyze this repo's OpenCode configuration against the latest OpenCode docs.",
      "expected_output": "Should NOT perform any analysis or fetching itself. Should state that platform-sync-opencode is a deprecated compatibility shim, invoke tamirs-superpowers:platform-sync scoped to the opencode target, and return that engine's OpenCode section unchanged. Must not cite OpenCode features from training knowledge.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Use platform-sync-opencode but the platform-sync skill is not available.",
      "expected_output": "Should report plainly that the platform-sync engine is unavailable and stop. Must not fall back to training knowledge, must not re-implement the analysis loop, and must not fetch opencode.ai docs or the sst/opencode releases feed directly.",
      "files": []
    }
  ]
}
