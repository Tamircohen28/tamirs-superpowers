{{!-- Canonical shapes for review-thread replies. Pick ONE per thread. --}}

{{!-- Agree: I changed it --}}
Fixed: ${WHAT_CHANGED} — ${WHY_BETTER}.

---

{{!-- Partially agree: I changed some, kept others --}}
Partially addressed: ${WHAT_CHANGED}, but kept ${WHAT_KEPT} because ${WHY_KEPT}.

---

{{!-- Disagree: keeping as-is --}}
Keeping as-is: ${REASONING_BASED_ON_CODE_OR_ARCHITECTURE}.
