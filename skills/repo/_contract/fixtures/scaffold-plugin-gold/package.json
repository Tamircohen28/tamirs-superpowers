{
  "name": "scaffold-plugin-gold",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "test": "npm run validate",
    "lint": "node -e \"process.exit(0)\"",
    "agent:check": "make agent:check",
    "agent:update": "echo 'Install to target repo — implement sync-to-repo.mjs in a follow-up'"
  }
}
