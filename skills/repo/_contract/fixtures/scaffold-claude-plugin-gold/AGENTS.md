# Minimal flat Claude plugin fixture — claude-plugin repo_type contract checks.

## Commands

| Command | Purpose |
|---------|---------|
| `make agent:check` | Verify thin adapters + feature equivalence + platform targets |
| `make agent-polish-gate` | Agent pre-PR gate (sync platform versions + assert + check) |

## Constraints

- Never commit secrets
