# Platform equivalence (fixture)

| Feature | Claude | Cursor | Codex |
|---------|--------|--------|-------|
| Hooks | hooks/hooks.json | Scoped .mdc rules | codex plugin hooks field |

Cursor has no hooks.json — use rules as substitute.
